using UnityEngine;

/// <summary>
///		Prevents this button from losing its custom colors (when the button color injector adds the default colors, this button will not be changed)
/// </summary>
public class IgnoreColorInject : MonoBehaviour {
}